# P005.1 — WinDetector Encapsulation and Final Closure

P005 implementation has been reviewed.

The game-rule behavior and test coverage are approved.

Before closing P005, perform one focused correction and verification.

## 1. Encapsulate WinningLines

The current implementation exposes:

```csharp
public static readonly int[][] WinningLines
```

This violates the intended encapsulation of the domain's immutable winning-rule definition because the inner arrays remain mutable.

Change the implementation so that the canonical winning combinations are not publicly mutable.

Prefer:

```csharp
private static readonly ...
```

or an equivalently immutable representation consistent with the project's C# version and conventions.

Do NOT expose the internal winning-line collection merely for testing.

Tests should verify behavior through `CheckWin`, not by depending on the internal implementation.

## 2. WinResult Defensive State

Inspect:

```text
WinResult.WinningCells
```

Ensure the result does not unintentionally expose mutable internal state.

The returned winning cells must behave as immutable/read-only domain data.

Do not introduce unnecessary abstractions.

## 3. Do Not Change Game Rules

Do not change:

* winning combinations
* turn behavior
* draw behavior
* terminal state behavior
* cell addressing
* move history
* exceptions

The P005 game-rule behavior is already approved.

## 4. Verify Tests

Run:

```text
dotnet build backend/TicTacToe.sln
dotnet test backend/TicTacToe.sln
npm test --prefix frontend -- --watch=false
```

Report exact results.

Expected:

```text
Backend:
66 or more passed
0 failed
0 unexpected skipped

Frontend:
2 or more passed
0 failed
```

The exact current numbers must be reported rather than assumed.

## 5. Verify Domain Purity

Confirm:

```text
TicTacToe.Domain
    ↓
no ASP.NET dependency
no Infrastructure dependency
no EF dependency
no HTTP dependency
no UI dependency
no external package dependency
```

## 6. Verify P005 Boundary

Confirm that P005 still does not contain:

```text
Memento
Undo
Strategy
ComputerPlayer
GameCompleted
Scoreboard
REST API
DTOs
Angular game behavior
Persistence
```

## 7. Verify Git Before Committing

Run:

```text
git status
git log --oneline -5
git show --stat HEAD
```

Determine whether the P005 implementation commit has already been created.

### If P005 implementation is already committed

Do NOT create another P005 implementation commit.

Create only a focused correction commit:

```text
fix(domain): encapsulate winning rule definitions
```

if a code change was necessary.

### If P005 implementation is not committed

Create the logical P005 implementation commit:

```text
feat(domain): implement game rules and win detection
```

Do not duplicate commits.

## 8. Audit Artifacts

Ensure:

```text
docs/ai/reviews/P005-review.md
docs/ai/IMPLEMENTATION-LOG.md
docs/ai/REQUIREMENT-TRACEABILITY.md
docs/ai/ARCHITECTURE-TRACEABILITY.md
```

accurately reflect the final state.

Do not claim human review.

Do not fabricate commit hashes.

## 9. Final Report

Return:

### Encapsulation Correction

What changed.

### WinResult

How its state is protected.

### Tests

Exact totals.

### Build

Exact result.

### Boundary Verification

Confirm deferred functionality remains deferred.

### Git

```text
Branch:
HEAD:
Commits created:
Working tree:
```

### Final Gate

Only if all checks pass:

```text
P005 PASS — READY FOR P006
```

Otherwise:

```text
P005 BLOCKED
```

Do not start P006.

STOP.
